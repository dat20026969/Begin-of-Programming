#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;
int main(){
    int a, b, c;
    cin>>a>>b>>c;
    if((a+b<=c)||(b+c<=a)||(c+a<=b)){
        return 0;
    }
    else{
       if(float)((a*a+b*b=c*c)||(b*b+c*c=a*a)||(c*c+a*a=b*b)){
            cout<<"tam giac vuong"<<endl;
       }
       else{
           return 0;
       }
    }
       return 0;
}